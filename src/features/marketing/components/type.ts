/* One scale for every section, so the sections read as one voice: Outfit
   (the display face) at weight 600, tight, left-aligned, on a measure that
   never runs past twenty characters; a secondary-grey Inter lead under it.
   Sections sit on the plain ground with the same air between them; there
   are no panels, the landing's shape is the blob (booklo-mark.tsx). */
export const H2 = "text-foreground font-display max-w-[20ch] text-[34px] leading-[1.06] font-semibold tracking-[-0.035em] text-balance sm:text-[42px] md:text-[52px]";
export const LEAD = "text-muted-foreground mt-4 max-w-[36rem] text-[17px] leading-relaxed text-balance sm:text-lg";
export const SECTION_INNER = "mx-auto w-full max-w-6xl px-5 sm:px-8";
export const SECTION = "pt-24 md:pt-32";
